---
import { education } from '../lib/site';
---
<div class="edu-grid">
  {
    education.map((e) => (
      <div class={`edu-card${e.current ? ' current' : ''}`}>
        {e.current && <span class="badge">Now</span>}
        <h3>{e.school}</h3>
        <div class="qual">{e.qualification}</div>
        <div class="when">
          {e.start ? `${e.start} – ${e.end}` : e.end}
          {e.location && ` · ${e.location}`}
        </div>
      </div>
    ))
  }
</div>
